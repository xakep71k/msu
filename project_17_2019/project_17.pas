{
Методы сортировки (зачетный проект)
}
type CatType = record
	name: string; { кличка }
	breed: string; { порода }
	rating: array[1..5] of integer; { пять оценок от 0 до 5 }
end;

program compare_sorting(stdin, stdout);
begin
end.

